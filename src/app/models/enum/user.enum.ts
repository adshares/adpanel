export enum userRolesEnum {
  ADMIN,
  ADVERTISER,
  PUBLISHER,
}

export enum reportType {
  CAMPAIGNS = 'campaigns',
  SITES = 'sites',
}

export enum reportState {
  PREPARING = 'preparing',
  READY = 'ready',
  DELETED = 'deleted',
}
